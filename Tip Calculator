print("Welcome to tip calculator")

total_bill = float(input("What is the total of the bill?"))
tip_total = int(input("What percentage tip would you like to give 10, 12, or 15?")) 
split_people = int(input( "How many people to split the bill ?"))
tip_math = (total_bill * tip_total / 100) + total_bill

result=tip_math / split_people

print(" Each person should pay " + str(round(result,2)))
